[project]
name = "youtube-transcript"
version = "0.1.0"
description = "Download and clean YouTube video transcripts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "yt-dlp>=2026.3.3",
]
